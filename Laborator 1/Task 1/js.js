const value = parseInt(prompt('Data'));

const monthValue = value * 12;
const dayValue = value * 365;
const hoursValue = value * 8766;

alert(`${monthValue} month, ${dayValue} month, ${hours2Value}`);